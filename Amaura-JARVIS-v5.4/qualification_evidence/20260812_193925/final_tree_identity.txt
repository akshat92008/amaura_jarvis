AMAURA JARVIS — CLOSE FINAL QUALIFICATION GAPS ONLY

STOP FEATURE DEVELOPMENT.

The latest evidence-first handoff is substantially better and honestly reports several UNVERIFIED areas.

Do NOT redesign JARVIS.
Do NOT add capabilities.
Do NOT refactor architecture.

Your task is ONLY to close the remaining qualification gaps and create a truly complete frozen audit package.

==================================================
1. EXPLAIN THE TEST COUNT CHANGE
==================================================

Previous independent collection reported approximately:

454 tests

Latest evidence reports:

451 collected
450 passed
1 skipped

Determine exactly why the collection count changed.

Produce:

qualification_evidence/<NEW_RUN_ID>/test_count_diff.md

Show:

previous test names if evidence exists
current complete collected test names
tests added
tests removed
tests renamed
tests deselected
collection/configuration differences

Do NOT say "normal drift."

Identify the exact three-test difference.

==================================================
2. IDENTIFY THE ACTUAL QUALIFIED SOURCE
==================================================

Current repository is DIRTY.

Therefore:

git rev-parse HEAD^{tree}

does NOT identify the complete source that passed the tests.

Before changing anything else, preserve the current working state.

Generate a canonical file manifest for every source-controlled/release-relevant file:

relative path
size
SHA256

Create:

WORKING_TREE_SHA256SUMS.txt

Also preserve:

git status --porcelain
git diff
git diff --cached
git rev-parse HEAD
git rev-parse HEAD^{tree}

The report must clearly distinguish:

HEAD_COMMIT
HEAD_TREE
WORKING_TREE_STATE

==================================================
3. CLOSE MEMORY RESTART PERSISTENCE
==================================================

Use a unique random qualification token.

Example:

AMAURA_RESTART_MEMORY_<UUID>

Procedure:

1. Start the canonical single JARVIS backend.
2. Save the test memory through the normal JARVIS memory path.
3. Verify it exists.
4. Record backend PID A.
5. Stop/restart backend through the canonical service mechanism.
6. Record backend PID B.
7. Ensure PID B != PID A.
8. Ask JARVIS to recall the token.
9. Verify it was retrieved from persisted memory rather than session history.

Also create a fresh session after restart.

PASS only if the token survives real process restart.

Provide raw evidence.

==================================================
4. CLOSE REAL ANTIGRAVITY QUALIFICATION
==================================================

Current status is:

REAL ANTIGRAVITY = UNVERIFIED

If Antigravity is intentionally disabled by configuration, enable it ONLY through the normal supported configuration path for this controlled test.

Do not weaken sandboxing.

Do not add unsafe permissions.

Create a completely fresh disposable Git repo.

Example:

def add(a, b):
    return a - b

with failing test:

assert add(2, 3) == 5

Commit baseline.

Then issue the coding request through REAL JARVIS.

Required real chain:

JARVIS
→ mission
→ Antigravity
→ actual agy process
→ isolated repository/worktree
→ modification
→ tests
→ evidence
→ QA/review
→ approval if required
→ completion

REQUIRED PROOF:

agy --version
agy executable path
actual PID
actual sanitized stdout/stderr
mission ID
task IDs
initial Git SHA
initial failing test
actual Git diff
verification command
independent test rerun
review evidence
final state
process cleanup

Absolutely NO:

DELETE FROM execution_runs
manual UPDATE work_items state
manual attempt reset
manual approval SQL

If real Antigravity cannot be configured without unsafe machine changes:

leave it UNVERIFIED.

Do not fake PASS.

==================================================
5. CLOSE LIVE OMNIROUTE QUALIFICATION
==================================================

Current status is:

REAL OMNIROUTE = UNVERIFIED

Check the existing private runtime configuration without printing credentials.

If valid credentials are currently configured, perform ONE harmless real request.

Record:

request timestamp
requested model
resolved model
provider
HTTP result
first token timestamp
completion timestamp
fallback_used
total duration

Redact credentials.

If credentials are not configured:

leave:

REAL OMNIROUTE = UNVERIFIED

Do NOT fabricate credentials.
Do NOT change/rotate secrets merely for qualification.

==================================================
6. REAL RESOURCE MEASUREMENT
==================================================

Discard 17.83 MB as a representation of whole-system memory.

Measure actual processes.

At minimum:

JARVIS backend RSS
Electron main RSS
Electron renderer RSS
total Electron RSS
all JARVIS-related RSS
idle CPU
system memory pressure
swap

Then separately measure peak during:

normal chat
macOS action
Antigravity real mission if available
Playwright/browser test if safely available

Use:

ps
vm_stat
memory_pressure
and/or equivalent reliable macOS process measurements.

Do not deliberately cause severe memory pressure.

Report totals in MB.

==================================================
7. VERIFY SINGLETON AGAIN DURING THESE TESTS
==================================================

Because real restart tests are being performed, collect PID snapshots:

initial
Desktop opened
memory restart
Antigravity mission
Desktop closed
Desktop reopened
final

For every snapshot prove:

number of jarvis.server processes
port owner
Electron processes
MissionRunner owner
Autopilot owner

No duplicate backend.

==================================================
8. FIX ONLY REPRODUCIBLE FAILURES
==================================================

If one of these proof tests reveals a real defect:

save failing evidence
add regression test
make minimal fix
rerun affected test
rerun complete final suite

No speculative changes.

==================================================
9. RUN FINAL SUITE AFTER LAST SOURCE CHANGE
==================================================

After the LAST production source modification:

PYTHONPATH=. .venv/bin/pytest --collect-only tests/

then:

PYTHONPATH=. .venv/bin/pytest --junitxml=<artifact> tests/

Record:

collected
passed
failed
errors
skipped
duration
exit code

Then prove no pytest/test worker remains.

Required:

0 failed
0 errors
exit 0
clean exit

==================================================
10. FREEZE THE EXACT SOURCE
==================================================

Only after qualification passes:

commit the final source if normal project policy allows it.

If committing:

record:

FINAL_COMMIT_SHA
FINAL_TREE_SHA

and require:

git status --porcelain

to be empty except explicitly excluded generated qualification/release artifacts.

If not committing:

produce a complete canonical working-tree source manifest and explicitly state that the release derives from that manifest.

Do not claim a dirty HEAD tree hash identifies the release.

==================================================
11. BUILD A GENUINELY COMPLETE AUDIT PACKAGE
==================================================

The previous audit ZIP was incomplete.

Do NOT package only:

jarvis/
tests/
docs/
scripts/

Create a snapshot of the COMPLETE release-relevant repository.

Include, as applicable:

jarvis/
tests/
docs/
scripts/
desktop-app/
.github/
Launch_Amaura.command
Launch_Amaura_Desktop.command
Install_*.command
Setup_*.command
pyproject.toml
VERSION
README
release/build scripts
all other release-relevant root files
qualification evidence

Exclude only:

.venv
.env*
credentials
runtime DB
cache
__pycache__
.pytest_cache
node_modules
temporary runtime files
secret logs

Create:

Amaura-JARVIS-v5.4.1-Independent-Audit-Package-FINAL.zip

Also create:

AUDIT_PACKAGE_FILELIST.txt
AUDIT_PACKAGE_SHA256SUMS.txt

==================================================
12. RELEASE PACKAGE HYGIENE
==================================================

Inspect archive content.

Prove no:

.env
.env.amaura
.env.amaura.local
API tokens
operator keys
runtime DB
.venv
node_modules
__pycache__
.pytest_cache
temporary test repositories
credential-bearing transcripts
stale v5.4.0 qualification evidence

==================================================
13. FINAL RESPONSE
==================================================

Return ONLY evidence-backed results.

AMAURA FINAL GAP-CLOSURE REPORT

RUN ID:

SOURCE
Version:
Final commit:
Final tree:
Dirty:
Working-tree manifest:

TEST COUNT EXPLANATION
Previous:
Current:
Exact reason for difference:

TESTS
Collected:
Passed:
Failed:
Errors:
Skipped:
Duration:
Exit:
Clean process exit:

MACOS REAL E2E:
PASS/FAIL

MEMORY AFTER REAL BACKEND RESTART:
PASS/FAIL/UNVERIFIED

REAL ANTIGRAVITY:
PASS/FAIL/UNVERIFIED

REAL OMNIROUTE:
PASS/FAIL/UNVERIFIED

BACKGROUND SINGLETON:
PASS/FAIL

RESOURCE MEASUREMENTS
Backend idle RSS:
Electron total RSS:
Whole Amaura idle RSS:
Normal chat peak:
Browser peak:
Antigravity peak:

CAPABILITIES
Real E2E:
Fixture:
Config only:
Not configured:
Fail:

FULL AUDIT PACKAGE:
absolute path

PACKAGE SIZE:
PACKAGE SHA256:

FINAL VERDICT:
FREEZE AND BEGIN DAILY USE
or
CONTROLLED TESTING ONLY
or
BLOCKED — FIX REQUIRED

Do not perform additional feature development.VERSION=5.4.1
COMMIT_SHA=8f10c54d1d6d166d2e120071c9b793a91829949a
TREE_SHA=3d1ba478d82e693c880a1b6a402ba36bc34803e6
DIRTY=true

Changed/Modified Files:
- Amaura-JARVIS-v5.4/docs/tool_audit_matrix.md
- Amaura-JARVIS-v5.4/jarvis/amaura/cognition.py
- Amaura-JARVIS-v5.4/tests/test_macos_app_control.py

Untracked Files:
- Amaura-JARVIS-v5.4/audit_script.py
- Amaura-JARVIS-v5.4/fix.py
- Amaura-JARVIS-v5.4/qualification_evidence/
- Amaura-JARVIS-v5.4/release/
- Amaura-JARVIS-v5.4/scripts/qual_harness.py
- Amaura-JARVIS-v5.4/scripts/qual_phase1.py
- Amaura-JARVIS-v5.4/scripts/qual_phase10.py
- Amaura-JARVIS-v5.4/scripts/qual_phase11.py
- Amaura-JARVIS-v5.4/scripts/qual_phase12_13_14.py
- Amaura-JARVIS-v5.4/scripts/qual_phase15_16_17.py
- Amaura-JARVIS-v5.4/scripts/qual_phase18_21.py
- Amaura-JARVIS-v5.4/scripts/qual_phase2.py
- Amaura-JARVIS-v5.4/scripts/qual_phase22_25.py
- Amaura-JARVIS-v5.4/scripts/qual_phase26_34.py
- Amaura-JARVIS-v5.4/scripts/qual_phase3.py
- Amaura-JARVIS-v5.4/scripts/qual_phase5.py
- Amaura-JARVIS-v5.4/scripts/qual_phase7.py
- Amaura-JARVIS-v5.4/scripts/qual_phase8.py
- Amaura-JARVIS-v5.4/scripts/qual_phase9.py
- Amaura-JARVIS-v5.4/test_chat.py
- Amaura-JARVIS-v5.4/test_direct_exec.py
- Amaura-JARVIS-v5.4/test_jarvis_run.py
- Amaura-JARVIS-v5.4/test_macos_apps_script.py
