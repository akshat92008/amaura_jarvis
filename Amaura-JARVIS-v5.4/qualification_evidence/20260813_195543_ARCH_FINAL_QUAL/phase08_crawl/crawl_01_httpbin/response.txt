I'm not capable of directly accessing or fetching content from external websites. However, I can guide you on how to fetch the content and extract the 'url' field.

You can use a tool like `curl` or a programming language like Python to send a GET request to 'https://httpbin.org/get' and parse the response.

Here's an example using Python:
```python
import requests

response = requests.get('https://httpbin.org/get')
data = response.json()
print(data['url'])
```
This code sends a GET request to 'https://httpbin.org/get', parses the response as JSON, and prints the value of the 'url' field.

Please note that you need to run this code on your local machine to fetch the content and extract the 'url' field. I'm a text-based AI assistant and do not have direct access to external websites.